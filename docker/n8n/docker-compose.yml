services:
  n8n:
    image: n8nio/n8n:latest
    container_name: n8n
    restart: unless-stopped
    ports:
      - "${N8N_PORT:-5678}:5678"
    environment:
      - N8N_HOST=${N8N_HOST}
      - N8N_PORT=5678
      - N8N_PROTOCOL=${N8N_PROTOCOL:-https}
      - N8N_EDITOR_BASE_URL=${N8N_EDITOR_BASE_URL:-https://n8n.ailinux.me}
      - N8N_PUBLIC_URL=${N8N_PUBLIC_URL:-https://n8n.ailinux.me}
      - N8N_ENDPOINT_REST=${N8N_ENDPOINT_REST:-/rest}
      - N8N_ENDPOINT_WEBHOOK=${N8N_ENDPOINT_WEBHOOK:-/webhook}
      - NODE_ENV=production
      - WEBHOOK_URL=${N8N_WEBHOOK_URL}
      - GENERIC_TIMEZONE=${TZ:-Europe/Berlin}
      # Auth
      - N8N_BASIC_AUTH_ACTIVE=${N8N_BASIC_AUTH_ACTIVE:-false}
      - N8N_BASIC_AUTH_USER=${TRIFORCE_ADMIN_USER}
      - N8N_BASIC_AUTH_PASSWORD=${TRIFORCE_ADMIN_PASSWORD}
      # AI Integration
      - N8N_AI_ENABLED=true
      # MCP Integration
      - MCP_API_URL=${MCP_API_URL}
      - OLLAMA_HOST=${OLLAMA_HOST}
    volumes:
      - ${TRIFORCE_DATA_DIR:-/home/zombie/workspace/triforce/data}/n8n:/home/node/.n8n
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - triforce-net
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:5678/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  triforce-net:
    name: ${TRIFORCE_NETWORK:-triforce-net}
    external: true
