[mysqld]
# Character set
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci

# Performance & reliability
innodb_buffer_pool_size = 1G
innodb_log_file_size = 256M
innodb_flush_log_at_trx_commit = 2
# innodb_flush_method removed: deprecated by current MariaDB; use server default
max_connections = 200
table_open_cache = 4000
tmp_table_size = 64M
max_heap_table_size = 64M

# Security
skip-name-resolve
bind-address = 0.0.0.0

# Logging
slow_query_log = 1
long_query_time = 2
# Leave log_error unset to inherit Docker image defaults (stderr)
# Increase verbosity for better diagnostics (0..3)

[client]
default-character-set = utf8mb4
