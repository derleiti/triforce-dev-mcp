[Service]
# The host-wide DefaultTimeoutStopSec is intentionally short, but dockerd must
# be allowed to honor per-container stop timeouts (mailserver uses 60 seconds).
# Killing dockerd after 5 seconds leaves stale netns/sandbox state across reboot.
TimeoutStopSec=120s
