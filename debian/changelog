triforce-backend (1:2.86.2-8) noble; urgency=medium

  * Packaging: preserve source-service running and enabled state across legacy
    2.86.0 upgrades whose old prerm stopped and disabled the service
  * Packaging: restore source mode before runtime/config initialization so
    package migration cannot take ownership of source-managed state

 -- Markus Leitermann <admin@ailinux.me>  Sun, 13 Sep 2026 00:20:00 +0200

triforce-backend (1:2.86.2-7) noble; urgency=medium

  * Models: classify Cohere/Cerebras 401/403 discovery failures as invalid
    credentials instead of advertising fallback models
  * Models: add provider discovery backoff and provider status reporting
  * Federation: make WireGuard proxy socket ordering reproducible in source
    with regression coverage

 -- Markus Leitermann <admin@ailinux.me>  Sun, 13 Sep 2026 00:14:00 +0200

triforce-backend (1:2.86.2-6) noble; urgency=medium

  * Preserve the operator-selected source service across package upgrades
  * Stop Debian maintainer scripts from auto-starting or restarting TriForce
  * Migrate legacy package-owned /etc systemd units without changing
    runtime mode
  * Record explicit source mode in /etc/triforce/source-mode.enabled
  * Keep package/source mode switching exclusively in install-service.sh
  * Packaging: exclude live secrets, agent state, caches and host symlinks
  * Packaging: use /etc/triforce as the packaged runtime config directory
  * Packaging: preserve source-owned config/state/log permissions on package
    upgrades
  * Packaging: isolate package metadata below /var/lib/triforce/package-runtime
  * Packaging: install tmpfiles policy for package runtime directories
  * Packaging: rotate TriForce log files with a 14-day compressed retention
    policy
  * Runtime: route TriStar mutable state through the canonical package state
    path

  * Packaging: align backend runtime version metadata with 2.86.2
  * Packaging: keep a canonical immutable service template for safe repair
  * Control Center: make the window heading release-neutral
 -- Markus Leitermann <admin@ailinux.me>  Sat, 12 Sep 2026 18:15:00 +0200

triforce-backend (1:2.86.1-1) noble; urgency=medium

  * Packaging: separate package and source service modes cleanly
  * Packaging: install package unit under /usr/lib/systemd/system
  * Packaging: run packaged backend as dedicated triforce service user
  * Packaging: keep mutable state and logs outside /opt/triforce
  * Packaging: resync Python dependencies when requirements change
  * Packaging: preserve explicit source-mode /etc systemd override on upgrade

 -- Markus Leitermann <admin@ailinux.me>  Sat, 12 Sep 2026 17:46:00 +0200

triforce-backend (2.86.0-1) noble; urgency=medium

  * Version 2.86.0 Release
  * Auth: X-Internal-Key support in central_auth_middleware
  * Auth: Request path logging for 401 debugging
  * Frontend: Dynamic model loading in Discuss widget (696 models)
  * Frontend: 10 high-contrast theme switcher presets
  * Frontend: Account dropdown js-ready class fix
  * Security: CSP admin exclusion in Apache vhost
  * Security: Hardcoded credentials externalized to wp-config.php
  * Security: REST auth bypass scope tightened
  * Performance: Duplicate CSS enqueue removed
  * Performance: Targeted cache flush replaces nuclear wp_cache_flush
  * Backup: Federation backup script with auto-detect, 2h interval
  * Backup: Deployed to all 3 federation nodes

 -- Markus Leitermann <admin@ailinux.me>  Sat, 11 Apr 2026 21:00:00 +0200
